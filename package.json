{
  "name": "apecom",
  "version": "1.0.0",
  "description": "Telegram bot using Telegraf and TypeScript",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "setup": "./scripts/dev.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["telegram", "bot", "telegraf", "typescript"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "telegraf": "^4.15.6",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "ts-node": "^10.9.1"
  }
}
